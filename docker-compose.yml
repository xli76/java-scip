services:
 java-app:
   build:
     context: .
     dockerfile: docker/Dockerfile.app
   ports:
     - "8080:8080"

#   scip-dep:
#     build:
#       context: .
#       dockerfile: docker/Dockerfile.dep
# #    volumes:
# #      - src/main/resources/lib:/var/lib
